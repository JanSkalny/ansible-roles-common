#XXX: generate via $( openssl rand -hex 20 )
#cortex_secret_key: "FIXME"
  
cortex_elasticsearch_uri: "http://127.0.0.1:9200"
cortex_elasticsearch_index: "cortex"

#cortex_ldap_server_uri: "{{ ldap_server }}:389"
#cortex_ldap_bind_dn: "uid=cortex,ou=tech,{{ ldap_base_dn }}"
#cortex_ldap_bind_password: "{{ ldap_tech_users['cortex'] }}"
#cortex_ldap_base: "{{ ldap_base_dn }}"

# custom analyzers and cortex-tools reside in
cortex_analyzers_dir: "/opt/cortex-analyzers"

# use analyzers from internet (with auto-updates)
cortex_analyzers_json: "https://download.thehive-project.org/analyzers.json"
cortex_analyzer_autoupdate: true

## use local analyzers definition (without auto-updates)
#cortex_analyzers_json: "{{ cortex_analyzers_dir }}/analyzers.json"
#cortex_analyzer_autoupdate: false
