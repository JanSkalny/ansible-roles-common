ca_dir: "{{ inventory_dir }}/storage/ansible-simple-pki/{{ default_ca_host }}"
type: "servers"
dest_name: "server"
ca_name: "ca.crt"
src_name_suffix: ".{{ simplepki_message_digest }}.{{ simplepki_key_size }}"
crt_name: "{{ dest_name }}.crt"
key_name: "{{ dest_name }}.key"
hostname: "{{ inventory_hostname }}" 
