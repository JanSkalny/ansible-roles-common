user = {{ mail_db_user }}
password = {{ mail_db_password }}
hosts = 127.0.0.1:3306
dbname = {{ mail_db_name }}

query = SELECT transport FROM domain WHERE domain='%s' AND (backupmx=1 OR transport='vacation')
