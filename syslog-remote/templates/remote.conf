{% if syslog_remote %}

# log everything to remote syslog server
destination d_remote_log1 { tcp("{{ syslog_remote }}" port(514) log_fifo_size(1000)); };
log { source(s_src); destination(d_remote_log1); };

{% endif %}
