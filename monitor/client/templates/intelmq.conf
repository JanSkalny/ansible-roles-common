{% raw %}
UserParameter=intelmq.logfile.discovery,/usr/bin/env python3 -c 'from pathlib import Path;import glob;import json;print(json.dumps({"data": [{"{#LOGFILE_PATH}": p, "{#LOGFILE_NAME}": ".".join(Path(p).name.split(".")[0:-1])} for p in glob.glob("/var/log/intelmq/*.log")]}, indent=2))'
{% endraw %}
