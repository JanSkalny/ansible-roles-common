cluster.name: "{{ elastic.cluster_name }}"

node.name: "{{ elastic.node_name }}"

path:
  logs: "{{ elastic.log_path }}"
  data: "{{ elastic.data_path }}"

bootstrap.memory_lock: true
network.host: "{{ elastic.host }}"
http.port: "{{ elastic.port }}"

