elastic:
  user: "elasticsearch"

  host: "127.0.0.1"
  port: "9200"

  cluster_name: "test"
  node_name: "{{ ansible_hostname }}"

  log_path: "/var/log/elasticsearch"
  data_path: "/var/data/elasticsearch"

kibana:
  host: "0.0.0.0"
  port: "5601"

